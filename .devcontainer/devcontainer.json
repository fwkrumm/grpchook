{
  "name": "Linux x64 DevContainer",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "runArgs": ["--net=host"],
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": [
    "ms-python.python",
    "ms-azuretools.vscode-docker"
  ],
  "postCreateCommand": "uv pip install --system -r requirements.txt && echo 'DevContainer setup complete!'"
}
