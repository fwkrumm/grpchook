NOTE that there are more examples in ../tests/integration. Those are also executed as part of the CI workflow and thus work deterministically but are rather basic.

The tests within THIS directory were tested locally but since they e.g. use specialized APIs (lm studio for example) are not trivial to test within the pipeline. Large parts of them are AI generated since I also used it to test how good AI would use the existing mechanics to create working clients/servers.
